import styles from "../styles/employee.module.css";

const Employee = () => {
  return (
    <div className={`${styles.main} main`}>
      <h1 className={styles.header}>This is Employee</h1>
    </div>
  );
};

export default Employee;
