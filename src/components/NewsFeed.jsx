import styles from "../styles/newsfeed.module.css";

const NewsFeed = () => {
  return (
    <div className={`${styles.main} main`}>
      <h1 className={styles.header}>This is NewsFeed</h1>
    </div>
  );
};

export default NewsFeed;
